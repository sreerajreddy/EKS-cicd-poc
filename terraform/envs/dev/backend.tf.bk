terraform {
  backend "s3" {
    bucket         = "poc-terraform-state-bucket"
    key            = "eks-poc/dev/terraform.tfstate"
    region         = "us-east-2"
    dynamodb_table = "terraform-locks"
    encrypt        = true
  }
}
