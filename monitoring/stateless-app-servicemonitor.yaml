apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: stateless-app-sm
  namespace: monitoring
  labels:
    release: kube-prom-stack
spec:
  namespaceSelector:
    matchNames:
      - apps
  selector:
    matchLabels:
      app: stateless-app
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
