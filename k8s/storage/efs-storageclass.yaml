apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: efs-sc

provisioner: efs.csi.aws.com

parameters:
  provisioningMode: efs-ap
  fileSystemId: fs-0becf1645a031d46e
  directoryPerms: "700"

reclaimPolicy: Retain
volumeBindingMode: Immediate
